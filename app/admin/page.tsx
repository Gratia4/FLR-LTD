"use client";
import { useCallback,useEffect,useMemo,useState } from "react";
import { CheckCircle,Clock,MagnifyingGlass,UsersThree,WarningCircle,XCircle } from "@phosphor-icons/react";
import { AdminShell } from "@/components/admin-shell";
import { apiRequest,ApiRequestError } from "@/lib/api";
import { useAdminSession } from "@/lib/use-admin-session";
type Status="PENDING"|"APPROVED"|"SUSPENDED"|"REJECTED";
type Registration={id:string;name:string;type:string;registration_number:string;tin:string;pharmacy_licence_number:string|null;operating_licence_number:string;district:string;country:string;status:Status;created_at:string;contact:{fullName:string;email:string;phone:string;jobTitle:string}};
const statuses:Status[]=["PENDING","APPROVED","SUSPENDED","REJECTED"];
export default function ApplicationsPage(){
 const{user,token,logout}=useAdminSession();const[records,setRecords]=useState<Registration[]>([]);const[filter,setFilter]=useState<Status|"ALL">("PENDING");const[search,setSearch]=useState("");const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[selected,setSelected]=useState<Registration|null>(null);const[reason,setReason]=useState("");const[updating,setUpdating]=useState(false);
 const load=useCallback(async()=>{if(!token)return;setLoading(true);setError("");try{const responses=await Promise.all(statuses.map(status=>apiRequest<{data:Registration[]}>(`/api/v1/admin/registrations?status=${status}`,{headers:{Authorization:`Bearer ${token}`}})));setRecords(responses.flatMap(item=>item.data))}catch(cause){setError(cause instanceof ApiRequestError?cause.message:"Applications could not be loaded.")}finally{setLoading(false)}},[token]);useEffect(()=>{load()},[load]);
 const count=(status:Status)=>records.filter(item=>item.status===status).length;const visible=useMemo(()=>records.filter(item=>(filter==="ALL"||item.status===filter)&&`${item.name} ${item.contact.fullName} ${item.contact.email} ${item.tin}`.toLowerCase().includes(search.toLowerCase())),[records,filter,search]);
 async function update(status:Status){if(!selected)return;if(["REJECTED","SUSPENDED"].includes(status)&&!reason.trim()){setError("Enter a decision note before restricting an account.");return}setUpdating(true);try{await apiRequest(`/api/v1/admin/registrations/${selected.id}/status`,{method:"PATCH",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({status,reason:reason||undefined})});setSelected(null);setReason("");await load()}catch(cause){setError(cause instanceof ApiRequestError?cause.message:"Status update failed.")}finally{setUpdating(false)}}
 return <AdminShell user={user} active="dashboard" onLogout={logout}><section className="admin-content"><div className="admin-title"><div><h2>Partner applications</h2><p>Review and manage wholesale account requests.</p></div><button onClick={load}>Refresh data</button></div>{error&&<div className="admin-error"><WarningCircle/>{error}</div>}<div className="metric-grid"><article><span className="amber"><Clock/></span><div><small>Pending review</small><strong>{count("PENDING")}</strong></div></article><article><span className="green"><CheckCircle/></span><div><small>Approved</small><strong>{count("APPROVED")}</strong></div></article><article><span className="red"><XCircle/></span><div><small>Rejected</small><strong>{count("REJECTED")}</strong></div></article><article><span className="blue"><UsersThree/></span><div><small>Total applications</small><strong>{records.length}</strong></div></article></div><section className="applications"><div className="table-tools"><div className="status-tabs"><button className={filter==="ALL"?"active":""} onClick={()=>setFilter("ALL")}>All</button>{statuses.map(status=><button key={status} className={filter===status?"active":""} onClick={()=>setFilter(status)}>{status.toLowerCase()} <b>{count(status)}</b></button>)}</div><label><MagnifyingGlass/><input value={search} onChange={event=>setSearch(event.target.value)} placeholder="Search company, contact or TIN"/></label></div><div className="admin-table"><div className="table-row table-head"><span>Business</span><span>Contact</span><span>Location</span><span>Submitted</span><span>Status</span><span></span></div>{loading?<div className="empty-state">Loading applications…</div>:visible.length===0?<div className="empty-state">No applications match this view.</div>:visible.map(item=><button className="table-row" key={item.id} onClick={()=>setSelected(item)}><span><b>{item.name}</b><small>{item.type.replaceAll("_"," ")} · TIN {item.tin}</small></span><span><b>{item.contact.fullName}</b><small>{item.contact.email}</small></span><span><b>{item.district}</b><small>{item.country}</small></span><span><b>{new Intl.DateTimeFormat("en-RW",{dateStyle:"medium"}).format(new Date(item.created_at))}</b><small>{item.contact.phone}</small></span><span><i className={`status ${item.status.toLowerCase()}`}>{item.status}</i></span><span className="review-link">Review →</span></button>)}</div></section></section>{selected&&<div className="review-backdrop"><aside className="review-panel"><button className="close-review" onClick={()=>setSelected(null)}>×</button><span className="review-eyebrow">Application review</span><h2>{selected.name}</h2><i className={`status ${selected.status.toLowerCase()}`}>{selected.status}</i><div className="review-grid"><label>Business type<b>{selected.type.replaceAll("_"," ")}</b></label><label>Registration no.<b>{selected.registration_number}</b></label><label>TIN<b>{selected.tin}</b></label><label>Operating licence<b>{selected.operating_licence_number}</b></label><label>District<b>{selected.district}, {selected.country}</b></label><label>Pharmacy licence<b>{selected.pharmacy_licence_number||"Not supplied"}</b></label></div><h3>Primary contact</h3><div className="contact-card"><b>{selected.contact.fullName}</b><span>{selected.contact.jobTitle}</span><a href={`mailto:${selected.contact.email}`}>{selected.contact.email}</a><a href={`tel:${selected.contact.phone}`}>{selected.contact.phone}</a></div><label className="decision-reason">Decision note<textarea value={reason} onChange={event=>setReason(event.target.value)} maxLength={500}/></label><div className="decision-actions"><button disabled={updating} className="approve" onClick={()=>update("APPROVED")}><CheckCircle/>Approve</button><button disabled={updating} className="suspend" onClick={()=>update("SUSPENDED")}>Suspend</button><button disabled={updating} className="reject" onClick={()=>update("REJECTED")}><XCircle/>Reject</button></div></aside></div>}</AdminShell>;
}
